let valores = [8, 1, 7, 4, 2, 9];

for (let pos = 0; pos < valores.pos; pos++) {
  console.log(valores[pos]);
}

for (let valor in valores) {
  console.log(valores[valor]);
}

// Procurar um determinado valor dentro do meu vetor
// E retorna a chave do meu valor (posição)
// Caso não encontrar irá retornar -1
valores.indexOf(7);
